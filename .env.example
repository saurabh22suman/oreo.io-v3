# Oreo.io Configuration Template
# Copy this file to .env and fill in your actual values
# NEVER commit .env files with real secrets to version control

# ================================
# SERVER CONFIGURATION
# ================================
PORT=8080
ENVIRONMENT=development  # Options: development, production

# ================================
# DATABASE CONFIGURATION
# ================================
# Choose ONE of the following:

# Option 1: PostgreSQL (production recommended)
# DATABASE_URL=postgres://username:password@localhost:5432/oreo?sslmode=disable

# Option 2: SQLite for metadata (migration path, recommended)
METADATA_DB=/data/meta/oreo.db

# ================================
# STORAGE CONFIGURATION
# ================================
# Delta Lake storage root directory
DELTA_DATA_ROOT=/data/delta

# Default storage backend: delta, postgres, or sqlite
DEFAULT_STORAGE_BACKEND=delta

# ================================
# PYTHON SERVICE
# ================================
PYTHON_SERVICE_URL=http://python-service:8000

# ================================
# SECURITY & AUTHENTICATION
# ================================
# JWT Secret - MUST be at least 32 characters
# Generate with: openssl rand -base64 32
JWT_SECRET=CHANGE_ME_TO_A_RANDOM_32_CHAR_STRING_OR_LONGER

# Session timeout in hours
SESSION_TIMEOUT_HOURS=24

# Cookie security (set to true in production with HTTPS)
COOKIE_SECURE=false

# Admin password - MUST be at least 12 characters
# Use a strong, unique password
ADMIN_PASSWORD=CHANGE_ME_TO_A_STRONG_PASSWORD

# ================================
# GOOGLE OAUTH (OPTIONAL)
# ================================
# Get these from Google Cloud Console
# GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
# GOOGLE_CLIENT_SECRET=your-secret

# ================================
# FEATURES
# ================================
# Disable background worker if needed
DISABLE_WORKER=false

# ================================
# PRODUCTION RECOMMENDATIONS
# ================================
# For production deployments:
# 1. Set ENVIRONMENT=production
# 2. Use strong, randomly generated secrets (32+ characters)
# 3. Use PostgreSQL for DATABASE_URL
# 4. Set COOKIE_SECURE=true when using HTTPS
# 5. Store this file securely and never commit it
# 6. Use environment-specific secret management (AWS Secrets Manager, etc.)
# 7. Rotate secrets regularly
# 8. Use strong ADMIN_PASSWORD (12+ chars, mixed case, numbers, special chars)
